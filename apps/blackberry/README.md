# Blackberry

Blackberry is the friends wedding website. See the [repository guide](../../README.md) for workspace commands, verification, and deployment.

Run this app with pnpm:

```bash
pnpm dev
```

It runs at http://localhost:3001. Edit the root route in `src/app/page.tsx`.
